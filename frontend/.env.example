# Production Frontend Environment Variables Example
# Leave empty or set to "/api" if serving backend via reverse proxy on the same domain
# Set to full backend URL (e.g. "https://test-score-api.onrender.com/api") for cross-domain deployment
VITE_API_BASE_URL="/api"
